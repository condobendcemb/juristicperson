services:
  # Web Service for Juristic Person App
  - type: web
    name: juristic-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python init_db.py && gunicorn app:app
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:Ben28122523!@db.sinskfbzszzuadgshxro.supabase.co:5432/postgres
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_APP
        value: app.py
      - key: FLASK_DEBUG
        value: "false"
